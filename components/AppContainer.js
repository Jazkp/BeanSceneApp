import React from "react";
import { View } from "react-native";

const AppContainer = ({ children }) => {
  return <View style={{ backgroundColor: "#f6e1c5" }}>{children}</View>;
};

export default AppContainer;
